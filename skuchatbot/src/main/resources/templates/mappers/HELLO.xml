<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.skuchatbot.Hello.HelloDAO">
    <!-- select 쿼리 (1개) -->
    <select id="HelloCheck" parameterType="com.example.skuchatbot.Hello.HelloDTO" resultType="com.example.skuchatbot.Hello.HelloDTO">
        select * from HELLO
    </select>
</mapper>