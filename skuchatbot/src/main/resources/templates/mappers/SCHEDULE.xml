<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.skuchatbot.SCHEDULE.ScheduleDAO">
    <!-- select 쿼리 (1개) -->
    <select id="ScheduleCheck" parameterType="com.example.skuchatbot.SCHEDULE.ScheduleDTO" resultType="com.example.skuchatbot.SCHEDULE.ScheduleDTO">
        select * from SCHEDULE
    </select>


</mapper>